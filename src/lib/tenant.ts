// Single-tenant for now, mirrors lib/core/config/tenant_config.dart in the
// Flutter app. Becomes a runtime value (restaurant switcher) once
// multi-tenant is built — nothing else needs to change to support that.
export const RESTAURANT_ID = 'e6323840-9644-471b-8964-203b76498a80';
